'use strict';

const models = {
    User: require('./user'),
    Post: require('./post'),
    Comment: require('./comment'),
    Log: require('./log'),
    Attendance: require('./attendance'),
};

module.exports = models;
